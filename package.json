{
  "name": "notionctl",
  "version": "0.1.3",
  "description": "Security-auditable, zero-dependency command-line interface for Notion, designed to be driven by AI coding agents via shell invocations.",
  "keywords": [
    "notion",
    "cli",
    "ai-agent",
    "copilot",
    "claude",
    "automation"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chazyua/notionctl.git"
  },
  "bugs": {
    "url": "https://github.com/chazyua/notionctl/issues"
  },
  "homepage": "https://github.com/chazyua/notionctl#readme",
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "bin": {
    "notionctl": "./bin/notionctl.js"
  },
  "files": [
    "bin/",
    "dist/src/",
    "README.md",
    "LICENSE",
    "SECURITY.md"
  ],
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean && node scripts/generate-version.js",
    "test": "npm run build && cd dist && node --test --test-reporter=spec",
    "test:unit": "npm run build && cd dist && node --test",
    "test:security": "npm run build && cd dist/test/security && node --test",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.2"
  }
}
